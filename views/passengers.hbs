
<div class="links-container">
	<a href="/">(Home)</a>
	<a href="passengers">(Passengers)</a>
	<a href="itineraries">(Itineraries)</a>
	<a href="airports">(Airports)</a>
	<a href="crew">(Crew)</a>
	<a href="planes">(Planes)</a>
	{{!-- <a href="MtoM.html">(M:M Relationships)</a> --}}
</div>

{{!-- Create a table --}}
<table id="passenger-table">

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.passenger_id}}</td>
            <td>{{this.name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.phone_num}}</td>
            <td>{{this.birth_date}}</td>
            <td>{{this.address}}</td>

        </tr>
        {{/
        
        
        each}}
    </tbody>
</table>

<!-- /views/index.hbs -->

{{!-- Form to add a record to the table  --}}
<h2>Adding Data with AJAX</h2>
<p>To add a new passengers, please enter their information below and click 'Submit'!</p>
<form id="add-passenger-form-ajax">
    <label for="name">Name: </label>
    <input type="text" name="name" id="name">
    
    <label for="email"> Email </label>
    <input type="text" name="email" id="email">

    <label for="phone_num">Phone Num: </label>
    <input type="text" name="phone_num" id="phone_num">

    <label for="birth_date">Birth Date </label>
    <input type="date" name="birth_date" id="birth_date">

    <label for="address">Address </label>
    <input type="text" name="address" id="address">

    <input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src="./js/add_passenger.js"></script>

